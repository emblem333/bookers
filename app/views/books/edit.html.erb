<p id="notice"><%= flash[:notice] %></p>
<!--<h1>Books#edit</h1><br>-->
<h1>Editing Book</h1><br>

<% if @book.errors.any? %>
  <%= @book.errors.count %> errors prohibited this book from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
<% end %>
  <!--,url: update_list_path(@list.id), method: :patch-->
  <%= form_with model: @book do |f| %>
  <div class="field">
    <label>Title</label>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <label>Body</label>
    <%= f.text_area :body %><br>
  </div>
  <%= f.submit "Update Book" %>
<% end %>

<%= link_to "Show", book_path(@book.id) %>
<%= "|" %>
<%= link_to "Back", '/books#index' %>